var test = require('tape');
var templates = require('./fixtures/templates.json');
var splitTree = require('../dist').splitTree;

test.skip('splitTree(Template)', function(t) {
  t.plan(0);
});

test.skip('splitTree(Template, { type })', function(t) {
  t.plan(0);
});

test.skip('splitTree(Template, { type, name })', function(t) {
  t.plan(0);
});